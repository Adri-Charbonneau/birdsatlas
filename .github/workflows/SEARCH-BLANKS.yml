name: SEARCH-BLANKS

on:
  #schedule:
    #- cron:  '0 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - name: Script
        run:  |
          ./SCRIPTS/SEARCH-BLANKS.ps1
